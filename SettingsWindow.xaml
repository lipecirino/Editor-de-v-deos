<Window x:Class="SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configurar Atalhos de Teclado"
        Height="400" Width="520" MinHeight="350" MinWidth="450"
        WindowStartupLocation="CenterOwner"
        WindowStyle="ToolWindow"
        ResizeMode="CanResize"
        Background="#FFF5F7FA"
        FontFamily="Segoe UI">
    
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF033150"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Personalize os atalhos de teclado" 
                   FontSize="16" FontWeight="SemiBold" Foreground="#FF033150" 
                   Margin="0,0,0,16"/>

        <!-- Lista de atalhos -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Cabeçalho da lista -->
            <Border Grid.Row="0" Background="#FFE8EEF4" CornerRadius="6,6,0,0" Padding="12,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Ação" FontWeight="Bold" Foreground="#FF264A66"/>
                    <TextBlock Grid.Column="1" Text="Tecla de Atalho" FontWeight="Bold" Foreground="#FF264A66" MinWidth="140" TextAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Linhas de configuração -->
            <ItemsControl Grid.Row="1" x:Name="listaAtalhos" BorderBrush="#FFD4DEE8" BorderThickness="1,0,1,1">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="12,0" BorderBrush="#FFE6EDF5" BorderThickness="0,0,0,1" Background="White">
                            <Grid MinHeight="42">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding NomeExibicao}" VerticalAlignment="Center" Foreground="#333" FontSize="13"/>

                                <Border Grid.Column="1" MinWidth="140" Height="30" Margin="0,6" 
                                        Background="#FFF1F4F8" CornerRadius="4" 
                                        BorderBrush="#FFCCD6E2" BorderThickness="1"
                                        Cursor="Hand" Tag="{Binding}"
                                        ToolTip="Clique e pressione a tecla desejada">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#FFE2E8F0"/>
                                                    <Setter Property="BorderBrush" Value="#FF3399FF"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <TextBlock x:Name="txtTecla" Text="{Binding TeclaExibicao}" 
                                                   VerticalAlignment="Center" HorizontalAlignment="Center"
                                                   Foreground="#FF033150" FontWeight="SemiBold" FontSize="13"/>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>

        <!-- Botões inferiores -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Name="btnRestaurarPadroes" Content="Restaurar Padrões" 
                    Background="#FF666666" Click="BtnRestaurarPadroes_Click"
                    Margin="0,0,10,0"/>
            <Button Name="btnSalvar" Content="Salvar" Click="BtnSalvar_Click" Margin="0,0,8,0"/>
            <Button Name="btnCancelar" Content="Cancelar" Background="#FF999999" Click="BtnCancelar_Click"/>
        </StackPanel>

        <!-- Dica -->
        <TextBlock Grid.Row="2" Text="Dica: Clique no campo da tecla e pressione a tecla desejada no teclado." 
                   FontSize="11" Foreground="#888" VerticalAlignment="Bottom" HorizontalAlignment="Left" Margin="0,16,0,0"/>
    </Grid>
</Window>
